{
  "name": "production-ready-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "echo 'Build step (e.g., TypeScript compilation)'",
    "test": "echo 'Running tests...' && exit 0",
    "lint": "echo 'Running linter...' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
