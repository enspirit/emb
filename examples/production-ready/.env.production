# Production environment variables
# This file shows what would be used in production
# (loaded by CI/CD or secrets manager in real deployments)

DOCKER_TAG=v1.0.0
NODE_ENV=production
LOG_LEVEL=warn
API_PORT=3000
WEB_PORT=80

# PostgreSQL configuration (external service, not an EMB component)
# In production, these would typically come from a secrets manager
POSTGRES_USER=app_prod
POSTGRES_PASSWORD=secure_production_password
POSTGRES_DB=production_ready
POSTGRES_PORT=5432

# Example: secrets would come from a secrets manager
# POSTGRES_PASSWORD=${vault:secret/prod/database#password}
# API_KEY=${vault:secret/prod/api#key}
