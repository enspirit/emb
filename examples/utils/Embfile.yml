# Not all folders are running components (docker services)
# You can also have Embfile with tasks that are always executed locally
tasks:
  release:
    script: | # shell
      echo "This will be executed locally and refuse --executor container"
